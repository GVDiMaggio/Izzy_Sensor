import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;

public class SensorArray
{
    public static void main(String[] args)
            throws IOException
    {
        Boolean input = false;
        int sensorValue = 0;
        int index = 0;
        int n = 0;

        ArrayList<SensorArray> SensorList = new ArrayList<>();

        if (n <= 15)
        {
            input = true;
        }
        {
            if (input = true)
            {
                SensorList.add(index, SensorArray);
            }


        }
        SensorList.get(sensorValue);
        SensorList.remove(2);

        Iterator ity = SensorList.iterator();
    }
}
